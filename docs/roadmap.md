## Future Roadmap

### Phase 2 — Firefox Support
- WXT natively targets Firefox; run `wxt build --browser firefox`
- Test content script + storage across both browsers

### Phase 3 — Share Improvements
- Optional: export generated CSS as a shareable snippet
- Community CSS library for common legacy site patterns

### Phase 4 — Theme Profiles
- Save named themes (e.g. "Dark Mode", "High Contrast") per site
- Switch between themes from popup

### Phase 5 — AI Model Expansion
- Gemini support
- Local model support (Ollama) for fully offline operation